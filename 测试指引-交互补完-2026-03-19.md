# 测试指引（交互补完）

适用范围：2026-03-19 交互补完改动（Phase 1~3），以及**布点管理 / ROI / 算法规则**相关后续迭代（预览版）。

---

## 布点管理（当前实现说明，测试前必读）

### 数据模型（预览版）

- **布点**：绑定**一个摄像头**；包含名称、启用状态等。
- **检测区域（ROI）**：挂在**布点级**（`deployment.rois`），**只画一次**，可被多条算法规则复用。
- **算法规则（原「算法实例」）**：每条规则包含：
  - 算法、版本模式（跟随/覆盖）、是否启用；
  - **生效 ROI**：通过 `roiIds` 引用上方已绘制的区域（可多选）；
  - **调度**：固定时间点 / 按频次（含重复周期、时间窗等）；
  - **检测参数**：置信度、触发次数、冷却、灵敏度、联动抓拍等。
- **检测参数仅在一处编辑**：在「编辑布点」主界面内当前选中规则的「检测参数」区块；**「新增/编辑规则」弹窗**仅维护算法与版本，不重复编辑检测参数（避免双入口混淆）。

### 编辑布点弹窗布局（三区，纵向分区，非分步向导）

1. **基础信息**：布点名称、启用、摄像头。
2. **检测区域（ROI）**：画布 + 区域列表；**无需先添加算法规则**即可新建矩形/多边形或示例区域。
3. **算法规则**：左侧规则列表；右侧为**当前选中规则**的「生效 ROI + 运行时间 + 检测参数」。

### 列表页

- 「规则摘要」列展示各规则的算法、ROI 数量、调度摘要（示意）。

---

## 1. 设备抓图测试

- 路径：设备管理 -> 摄像头 -> 抓图测试。
- 操作：
  - 点击“开始抓图”，观察加载、结果预览、成功提示。
  - 连续触发多次，验证失败场景会出现错误信息。
  - 在失败后点击“重试”。
- 预期：
  - 最近成功时间、最近失败原因、最近记录列表可见且更新。
  - 重试可再次触发抓图流程。

---

## 2. 布点管理：编辑布点（ROI + 算法规则）

- 路径：**算法与布点 -> 布点管理** -> 新增布点 / 编辑布点。
- **推荐操作顺序（逻辑上，界面不强制分步）**：
  1. 填好**基础信息**并选择摄像头；
  2. 在 **检测区域（ROI）** 中绘制或添加区域，命名、启用/删除单个区域；
  3. 在 **算法规则** 中「新增规则」，在弹窗中选择算法与版本并保存；
  4. 在左侧选中该规则，在右侧勾选**生效 ROI**、配置调度与**检测参数**；
  5. 可再新增第二条规则，对**同一 ROI** 勾选不同算法，无需重复绘制区域。
- 补充操作：
  - 在规则列表中切换选中行，右侧应切换为对应规则的配置。
  - 「算法」链接打开弹窗：仅改算法/版本/启用；保存后参数仍以主界面为准。
  - 规则级「参数重置」「调度重置」（批量）与单条规则内「检测参数一键重置」。
- 预期：
  - ROI 与规则分属不同分区，语义清晰；未选规则时右侧有空态提示。
  - 无算法规则时仍可新建 ROI；新建 ROI 后若已选中某条规则，新区域可自动加入该规则勾选（可在规则中取消）。
  - 保存成功有提示；再次打开编辑，ROI 与规则配置可回显（演示存储以本地为准）。

---

## 3. 布点列表与其它操作

- 路径：布点管理列表。
- 操作：
  - 查看「规则摘要」列内容是否合理。
  - 启用/停用、暂停/恢复运行、模拟告警、巡检恢复、删除等（演示逻辑）。
- 预期：
  - 列表与编辑弹窗中的规则/ROI 含义一致；模拟告警能关联到布点与 ROI（演示数据）。

---

## 4. 算法管理基础交互

- 路径：算法管理。
- 操作：
  - 新增算法 -> 保存。
  - 再次新增同名算法。
  - 对算法执行启用/停用、编辑、删除。
- 预期：
  - 新增/更新/启停/删除均有明确反馈。
  - 同名算法新增被拦截并提示。

---

## 5. 任务同步与图片同步

- 路径：任务管理。
- 操作：
  - 点击“同步任务”与“图片同步”。
  - 观察顶部同步阶段卡片。
  - 对同步失败的任务点击“重试同步”。
- 预期：
  - 阶段状态按空闲 -> 拉取/写入 -> 完成或失败变化。
  - 失败时展示错误原因，可手动重试。

---

## 6. 报警设置推送任务

- 路径：AI预警 -> 报警设置。
- 操作：
  - 查看推送任务表。
  - 使用“通道/状态”筛选。
  - 对失败任务点击“重试”。
- 预期：
  - 统计摘要（总数/成功/发送中/失败）与筛选结果一致。
  - 失败重试后状态可更新。

---

## 7. 报警记录页一致性

- 路径：AI预警 -> 报警记录。
- 操作：
  - 切换列表/宫格。
  - 设置极端筛选条件，观察无数据状态。
- 预期：
  - 顶部显示最近刷新状态。
  - 无数据时出现空态提示，不出现空白表格。

---

## 8. 日志与系统管理统一

- 路径：
  - 日志中心（任一日志页）
  - 系统管理 -> 人员管理
  - 系统管理 -> 配置管理
- 操作：
  - 点击导出按钮。
  - 点击人员/配置导入按钮。
- 预期：
  - 导出会下载 JSON 文件（演示数据）。
  - 导入给出演示预检提示，不覆盖现有数据。

---

## 附录：已废弃 / 不再作为布点主流程的说明

- 早期文档中「布点内按实例各自绘制 ROI」或「摄像头页独立 ROI 配置与布点强绑定」的描述**已不适用**当前预览版；请以**布点编辑弹窗内「布点级 ROI + 规则引用 roiIds」**为准。
